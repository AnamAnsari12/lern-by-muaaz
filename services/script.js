// const container = document.querySelector(".flex-container");
// container.addEventListener("wheel", (e) => {
//   e.preventDefault();
//   container.scrollLeft += e.deltaY; // Adjust scrolling speed
// });
